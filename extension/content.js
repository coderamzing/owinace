// Content script - platform classes are loaded via manifest.json content_scripts
// Platform classes are available globally after scripts load
// No initialization needed - popup.js will directly use platform classes via scripting.executeScript
